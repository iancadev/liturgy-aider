Outstanding tasks list:

Somewhat important for text-setting:
- [x] Three-column layout
- [ ] Two-tier indent (for numbered verses + lines inside verse)
- [ ] Make the preview update if any of the referenced images update.
- [ ] Tailwind styles (for customization --- style classes should propagate)

Also:
- [ ] Add a menu on every page (one-click switch between tabs)
- [ ] Fix the event-listener leak (all the fileWatchers)
- [ ] Clear the static/generated-images on project quit?
- [ ] Make the CSS field selectors use numbers if the is="???" is not unique
- [ ] Show the code for reference in the browser (don't have to copy-paste)
- [ ] Left-quote, right-quote?
- [ ] Make ! ignored or not in the browser (using a checkbox)

Final nuclear option: just ask Codex to identify and patch bugs / flimsiness in the architecture.